# A tuple is a collection of items stored in a single variable
#ordered,immutable(meaning it cannot be changed),Allows duplcates
students_names=("James","Jane","Alice")
print(students_names)

print(students_names[0])
print(len(students_names))
print("Alice" in students_names)